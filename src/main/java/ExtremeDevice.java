public class ExtremeDevice extends Device {
    public ExtremeDevice(String name, int minAge, float minWeight, float minHeight, float cost) {
        super(name, minAge, minWeight, minHeight, cost);
    }
}